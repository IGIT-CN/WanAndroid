<?xml version="1.0" encoding="utf-8"?>
<layout>

    <data>

        <variable
            name="item"
            type="com.zhuzichu.android.wan.ui.file.viewmodel.ItemViewModelFileNav" />
    </data>

    <LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:background="?android:attr/selectableItemBackground"
        android:clickable="true"
        android:focusable="true"
        android:gravity="center"
        app:onClickCommand="@{item.onClickItem}">

        <androidx.appcompat.widget.AppCompatTextView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_gravity="center"
            android:drawableStart="@drawable/ic_file_picker_arrow_right"
            android:drawablePadding="16dp"
            android:ellipsize="middle"
            android:gravity="center"
            android:maxEms="12"
            android:paddingStart="16dp"
            android:paddingEnd="16dp"
            android:singleLine="true"
            android:text="@{item.fileName}"
            android:textAllCaps="false"
            android:textColor="#FF48282c"
            android:textSize="16sp"
            android:textStyle="bold"
            tools:text="SD 存储卡SD 存储卡SD 存储卡SD 存储卡" />
    </LinearLayout>
</layout>